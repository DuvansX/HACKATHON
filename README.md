# IDEAPRO

Proyecto web estático organizado por componentes.

## Ejecutar localmente

Abre `public/index.html` en un servidor web local, o instala Firebase CLI y ejecuta `npm run serve`.

## Desplegar Firebase

Ejecuta `npm run deploy`. La configuración y las reglas se encuentran en `src/config/`.
